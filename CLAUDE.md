# LoBoost — Legacy Pointer

Este documento se mantiene por compatibilidad histórica.

## Fuente de verdad actual

- Reglas runtime: `.cursor/rules/*.mdc`
- Skills de ejecución: `.cursor/skills/*/SKILL.md`
- Onboarding humano: `AGENTS.md`
- Decisiones persistentes: `docs/decisions/*.md`

## Nota

Si este archivo contradice reglas en `.cursor/rules`, prevalecen las reglas de `.cursor/rules`.
